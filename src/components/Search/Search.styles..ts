import { Space } from 'antd';
import styled from 'styled-components';

const StyledSearch = styled(Space.Compact)`
  && {
    width: 100%;

    .ant-form {
      width: 100%;
    }
  }
`;

export default StyledSearch;
